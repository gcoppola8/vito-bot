name: Trigger auto deployment for vitobot

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '**'
    - '.github/workflows/vitobot-AutoDeployTrigger-9f1fcca9-cd57-4fc9-9830-ef1a4cc61a05.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.VITOBOT_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.VITOBOT_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.VITOBOT_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/VitoBot
          _dockerfilePathKey_: _dockerfilePath_
          registryUrl: coppola-gdg9drg5gshmh8a5.azurecr.io
          registryUsername: ${{ secrets.VITOBOT_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.VITOBOT_REGISTRY_PASSWORD }}
          containerAppName: vitobot
          resourceGroup: Steam
          imageToBuild: coppola-gdg9drg5gshmh8a5.azurecr.io/vitobot:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


